export const page = String.raw`<!doctype html>
<html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>CuetScript · 故事工作台</title><style>
:root{font-family:system-ui,-apple-system,"Noto Sans CJK SC",sans-serif;color:#24312c;background:#f4f5f1;font-size:16px;line-height:1.6}*{box-sizing:border-box}body{margin:0}header{background:#173e32;color:white;padding:20px max(20px,calc((100vw - 1100px)/2));display:flex;justify-content:space-between;align-items:center;gap:12px}h1{font-size:23px;margin:0}h2{font-size:20px;margin:0 0 16px}h3{font-size:16px}header p{margin:0;color:#c4d5c7;font-size:13px}main{max-width:1100px;margin:auto;padding:22px 20px 60px}.card{background:white;border:1px solid #dce2d9;border-radius:12px;padding:22px;margin-bottom:18px}.row{display:flex;align-items:end;gap:12px;flex-wrap:wrap}.row>*{flex:1;min-width:130px}.row>button{flex:0 0 auto}label{display:block;font-size:14px;font-weight:600;margin-bottom:12px}input,select,textarea,button{font:inherit}input,select,textarea{display:block;width:100%;border:1px solid #b9c5bb;border-radius:7px;padding:10px;background:#fff;color:inherit;margin-top:5px}textarea{resize:vertical;min-height:100px}button{border:1px solid #b9c5bb;border-radius:7px;padding:10px 16px;background:#fff;color:#244d3c;cursor:pointer;min-height:44px}button.primary{background:#245b45;color:white;border-color:#245b45}button:disabled{opacity:.5;cursor:wait}button.danger{color:#a23832}button:focus-visible,input:focus-visible,textarea:focus-visible,select:focus-visible{outline:3px solid #92b9ff;outline-offset:2px}nav{display:flex;gap:6px;overflow-x:auto;margin:18px 0}nav button{white-space:nowrap;border:0;background:transparent}nav button.active{background:#dce9df;color:#173e32;font-weight:700}.muted,small{color:#66746a;font-size:13px}.badge{background:#e9efe9;border-radius:20px;padding:4px 10px;font-size:12px}pre,.prose{white-space:pre-wrap;overflow-wrap:anywhere;word-break:break-word}pre{font-size:13px;background:#f5f6f3;padding:12px;border-radius:8px;max-height:440px;overflow:auto}.prose{font-family:serif;font-size:18px;line-height:1.95}.split{display:grid;grid-template-columns:1fr 1fr;gap:18px}.hidden,[hidden]{display:none!important}#notice{position:sticky;top:8px;background:#fff3dc;border:1px solid #e3c98d;border-radius:8px;padding:10px 14px;z-index:2;margin-bottom:12px;overflow-wrap:anywhere}#editor{font-family:ui-monospace,monospace;min-height:390px;font-size:14px}.turn{padding:15px 0;border-top:1px solid #e0e5df;overflow-wrap:anywhere}.turn button{margin:5px 5px 0 0;font-size:14px}.turn p{margin:5px 0}.hint{border-left:3px solid #b7caba;padding-left:12px}.actions{display:flex;gap:8px;flex-wrap:wrap}details summary{cursor:pointer;color:#42604b}#jobbar{border-left:4px solid #357955}#jobbar pre{max-height:150px}.file-controls{display:grid;grid-template-columns:1fr auto;gap:10px}@media(max-width:650px){header{padding:16px;align-items:start}header .badge{display:none}main{padding:14px 12px 40px}.card{padding:16px;border-radius:9px}.split{grid-template-columns:1fr;gap:0}.row{gap:6px}.row>label{min-width:100%;margin-bottom:8px}.row>button{flex:1}nav{margin:12px 0}nav button{padding:10px 13px}#editor{min-height:340px;font-size:16px}.file-controls{grid-template-columns:1fr}.prose{font-size:17px}h1{font-size:21px}}
</style></head><body><header><div><h1>CuetScript</h1><p>故事工作台 · 本地多模型叙事</p></div><span class="badge">CLI 开发版 · Web 界面</span></header><main>
<div id="notice" role="status" hidden></div>
<section id="login" class="card"><h2>连接工作台</h2><p class="muted">输入启动服务时显示的访问口令。服务商密钥只在服务器本地读取。</p><form id="loginForm"><label>访问口令<input id="token" type="password" required autocomplete="current-password"></label><button class="primary">连接</button></form></section>
<div id="workspace" hidden><section class="card"><div class="row"><label>当前故事<select id="stories" aria-label="当前故事"></select></label><button id="refresh">刷新</button></div><div id="storyInfo" class="muted"></div><details><summary>创建新故事</summary><form id="createForm" class="row"><label>目录名称（英文、数字、- 或 _）<input id="newStory" required pattern="[a-zA-Z0-9][a-zA-Z0-9_-]{0,63}" placeholder="my-story"></label><button>创建示例故事</button></form></details></section>
<section id="jobbar" class="card" hidden><div class="row"><strong id="jobTitle"></strong><button id="cancel" class="danger">停止运行</button></div><pre id="progress" aria-live="polite"></pre><p class="muted">关闭网页不会中止后台任务。停止后可到运行记录中恢复。</p></section>
<nav aria-label="工作区"><button data-tab="write" class="active">写故事</button><button data-tab="history">运行记录</button><button data-tab="files">设定与正文</button><button data-tab="lore">知识检索</button><button data-tab="settings">服务与分支</button></nav>
<section data-panel="write"><div class="split"><form id="runForm" class="card"><h2>推进一轮</h2><label>角色行动或台词<textarea id="roleplay" placeholder="我走进接待室，向林灯询问访问手续。"></textarea></label><label>剧情方向<textarea id="direction" placeholder="解释制度，在需要我回答时停下。"></textarea></label><label>写作要求<input id="authorNote" placeholder="约300字，不替我的角色作答。"></label><details><summary>与系统交流（OOC）</summary><label>系统交流<textarea id="ooc" placeholder="不属于角色台词的交流"></textarea></label></details><p class="muted">生成包含知识检索，会使用已配置的模型服务并产生用量。</p><button class="primary" data-start>开始生成</button></form><section class="card"><h2>生成结果</h2><div id="prose" class="prose">生成完成后，正文会显示在这里。</div><details><summary>结果详情</summary><pre id="result"></pre></details></section></div></section>
<section data-panel="history" hidden><div class="card"><div class="row"><h2>运行记录</h2><button id="loadTurns" data-start>刷新记录</button></div><p class="muted">完成的轮次可直接阅读；未完成的轮次可恢复。恢复需要切换到它原来的分支。</p><label>恢复时额外增加的秒数（可选）<input id="additionalSeconds" type="number" min="1" max="86400" placeholder="不增加"></label><label><input id="retryProtocol" type="checkbox" style="display:inline;width:auto"> 已修复协议问题，恢复时重置纠错次数</label><div id="turns"></div></div></section>
<section data-panel="files" hidden><div class="card"><h2>设定与正文</h2><p class="muted">设定使用 YAML / JSON。保存即创建故事版本；正文为只读。新增文件使用英文路径，如 lore/harbor.yaml。</p><div class="file-controls"><label>故事文件<select id="fileList"></select></label><button id="loadFile">加载文件</button></div><label>文件路径<input id="filePath" placeholder="lore/new-place.yaml"></label><label>内容<textarea id="editor" spellcheck="false"></textarea></label><div class="actions"><button id="saveFile" class="primary" data-start>保存并提交</button><button id="newFile">新建 Lore</button><button id="deleteFile" class="danger" data-start>删除此设定</button></div><p id="fileState" class="muted"></p></div></section>
<section data-panel="lore" hidden><div class="card"><h2>知识检索</h2><p class="muted">调用 Embedding 与 Reranker，返回来源与评分。故事生成也会自动建立或复用索引。</p><form id="searchForm"><label>检索问题<input id="query" required placeholder="没有凭证如何申请进入白塔？"></label><div class="actions"><button class="primary" data-start>检索</button><button type="button" id="indexLore" data-start>建立 / 更新索引</button></div></form><pre id="loreResult">尚未检索</pre></div></section>
<section data-panel="settings" hidden><div class="split"><div class="card"><h2>模型服务</h2><p id="credentials" class="muted"></p><p class="hint muted">模型、预算和角色控制规则在“设定与正文 → config/cuet.yaml”中修改。Antigravity 为实验性接入；账号导入在服务启动前由本地命令完成。</p><div class="actions"><button id="doctor" data-start>本地检查</button><button id="models" data-start>Gemini 模型列表</button></div><label>测试服务<select id="provider"><option value="deepseek">DeepSeek</option><option value="antigravity">Antigravity</option></select></label><label>Gemini 模型<input id="model" value="gemini-3-flash"></label><button id="checkProvider" data-start>真实工具往返测试（产生用量）</button><pre id="serviceResult"></pre></div><div class="card"><h2>故事分支</h2><label>已有分支<select id="branches"></select></label><button id="switchBranch" data-start>切换分支</button><form id="branchForm"><label>新分支名称<input id="branchName" required placeholder="alternative"></label><label>起点版本<input id="branchFrom" required placeholder="完整或简短提交标识"></label><button data-start>创建并切换</button></form><h3>最近版本</h3><pre id="commits"></pre></div></div></section>
</div></main><script>
const $=id=>document.getElementById(id);let token=sessionStorage.getItem('cuetToken')||'',current='',storyState=null,fileHead='',fileDirty=false,activeJob=null,pollTimer=null;
const hash=new URLSearchParams(location.hash.slice(1));if(hash.get('token')){token=hash.get('token');history.replaceState(null,'',location.pathname);}$('token').value=token;
function notice(text){$('notice').textContent=text;$('notice').hidden=!text;}
async function api(path,data){const r=await fetch('/api/'+path,{method:data?'POST':'GET',headers:{Authorization:'Bearer '+token,...(data?{'Content-Type':'application/json'}:{})},body:data?JSON.stringify(data):undefined});const v=await r.json();if(!r.ok)throw Error(v.message||v.error||'请求失败');return v;}
function safe(fn){return async e=>{e?.preventDefault();try{await fn(e);}catch(error){notice(error.message);}};}
function restoreDraft(){let draft={};try{draft=JSON.parse(sessionStorage.getItem('cuetDraft:'+current)||'{}');}catch{}for(const id of ['roleplay','direction','authorNote','ooc'])$(id).value=draft[id]||'';}
for(const id of ['roleplay','direction','authorNote','ooc'])$(id).addEventListener('input',()=>{sessionStorage.setItem('cuetDraft:'+current,JSON.stringify(Object.fromEntries(['roleplay','direction','authorNote','ooc'].map(k=>[k,$(k).value]))));});
function opts(select,values,selected){select.replaceChildren(...values.map(v=>{const o=document.createElement('option');o.value=v;o.textContent=v;return o;}));if(values.includes(selected))select.value=selected;}
function tab(name){document.querySelectorAll('[data-panel]').forEach(e=>e.hidden=e.dataset.panel!==name);document.querySelectorAll('[data-tab]').forEach(e=>e.classList.toggle('active',e.dataset.tab===name));}
document.querySelectorAll('[data-tab]').forEach(e=>e.onclick=()=>tab(e.dataset.tab));
function busy(value){document.querySelectorAll('[data-start]').forEach(e=>e.disabled=value);$('createForm').querySelector('button').disabled=value;}
async function refresh(){const state=await api('stories');opts($('stories'),state.stories,current);current=$('stories').value;sessionStorage.setItem('cuetStory',current);$('credentials').textContent='本地凭据配置：'+Object.entries(state.credentials).map(([k,v])=>k+' '+(v?'已提供':'未提供')).join(' · ');if(current){storyState=await api('story?story='+encodeURIComponent(current));$('storyInfo').textContent='分支 '+storyState.branch+' · 版本 '+storyState.head.slice(0,8)+(storyState.dirty?' · 有未提交修改，请先在本地处理':' · 已保存');opts($('fileList'),storyState.files,$('fileList').value);opts($('branches'),storyState.branches,storyState.branch);$('commits').textContent=storyState.commits;$('branchFrom').value=storyState.head;}else{$('storyInfo').textContent='还没有故事，请先创建一个示例故事。';storyState=null;}
const running=state.jobs.find(j=>j.story===current&&j.status==='running');if(running){watch(running);}else if(activeJob&&activeJob.story!==current){clearTimeout(pollTimer);activeJob=null;$('jobbar').hidden=true;busy(false);} }
async function connect(){await api('stories');sessionStorage.setItem('cuetToken',token);$('login').hidden=true;$('workspace').hidden=false;current=sessionStorage.getItem('cuetStory')||'';await refresh();restoreDraft();notice('');}
$('loginForm').onsubmit=safe(async()=>{token=$('token').value.trim();await connect();});
$('refresh').onclick=safe(refresh);$('stories').onchange=safe(async()=>{if(fileDirty&&!confirm('尚有未保存的编辑，是否切换故事并放弃这些编辑？')){$('stories').value=current;return;}fileDirty=false;$('editor').value='';$('prose').textContent='选择记录阅读，或开始新一轮。';$('turns').replaceChildren();current=$('stories').value;fileHead='';await refresh();restoreDraft();});
async function action(action,extra={}){if(!current)throw Error('请先创建或选择故事。');notice('');const job=await api('action',{story:current,action,...extra});if(job.id)watch(job);else {await refresh();notice('操作完成');}return job;}
function showResult(job){const value=job.result;const text=typeof value==='string'?value:JSON.stringify(value,null,2);if(job.status!=='succeeded'){notice((value?.message||value?.error||'操作未完成')+'；可在运行记录中检查与恢复。');return;}
if(['run','resume','regenerate'].includes(job.action)){$('prose').textContent=value?.content||text;$('result').textContent=text;tab('write');}
else if(job.action==='turns')renderTurns(value);
else if(['search','index'].includes(job.action))$('loreResult').textContent=text;
else if(['doctor','models','check'].includes(job.action))$('serviceResult').textContent=text;
}
function watch(job){clearTimeout(pollTimer);activeJob=job;$('jobbar').hidden=false;busy(job.status==='running');$('cancel').hidden=job.status!=='running';$('jobTitle').textContent=({'run':'故事生成','resume':'恢复运行','regenerate':'重新生成','turns':'读取记录','init':'创建故事','search':'知识检索','index':'建立索引','check':'服务测试','doctor':'检查环境','models':'获取模型','branch':'创建分支'}[job.action]||job.action)+' · '+({'running':'进行中','succeeded':'完成','failed':'失败','cancelled':'已停止'}[job.status]);$('progress').textContent=job.progress||(job.status==='running'?'正在处理…':'操作已结束。');if(job.status==='running')pollTimer=setTimeout(()=>poll(job.id),1000);}
async function poll(id){try{const job=await api('job?id='+encodeURIComponent(id));watch(job);if(job.status!=='running'){showResult(job);activeJob=null;if(job.action==='init'&&job.status==='succeeded')current=job.story;await refresh();}}catch(e){notice('连接中断，正在重试。'+e.message);pollTimer=setTimeout(()=>poll(id),3000);}}
$('cancel').onclick=safe(async()=>{if(activeJob){await api('cancel',{id:activeJob.id});notice('已请求停止，正在保存运行记录。');}});
$('createForm').onsubmit=safe(async()=>{const name=$('newStory').value.trim();const job=await api('action',{story:name,action:'init'});watch(job);});
$('runForm').onsubmit=safe(()=>action('run',{roleplay:$('roleplay').value,direction:$('direction').value,authorNote:$('authorNote').value,ooc:$('ooc').value}));
$('loadTurns').onclick=safe(()=>action('turns'));
function renderTurns(turns){$('turns').replaceChildren();if(!turns.length){$('turns').textContent='还没有运行记录。';return;}for(const t of [...turns].reverse()){const box=document.createElement('div');box.className='turn';const title=document.createElement('strong');title.textContent=(t.result?'已完成':'待完成')+' · '+t.id;const p=document.createElement('p');p.className='muted';p.textContent=t.branch+' · '+(t.input?.roleplay||t.input?.ooc||t.input?.direction||'');box.append(title,p);const resume=document.createElement('button');resume.textContent=t.result?'阅读结果':'恢复运行';resume.onclick=safe(()=>action('resume',{turn:t.id,additionalSeconds:$('additionalSeconds').value,retryProtocol:$('retryProtocol').checked}));const regen=document.createElement('button');regen.textContent='在新分支重生成';regen.onclick=safe(async()=>{const branch=prompt('新的故事分支名称');if(branch)await action('regenerate',{turn:t.id,branch});});box.append(resume,regen);$('turns').append(box);}}
$('searchForm').onsubmit=safe(()=>action('search',{query:$('query').value}));$('indexLore').onclick=safe(()=>action('index'));
$('doctor').onclick=safe(()=>action('doctor'));$('models').onclick=safe(()=>action('models'));$('checkProvider').onclick=safe(()=>action('check',{provider:$('provider').value,model:$('model').value}));
$('switchBranch').onclick=safe(async()=>{if(fileDirty)throw Error('请先保存或重新加载当前编辑。');await action('switch',{branch:$('branches').value});});$('branchForm').onsubmit=safe(()=>action('branch',{branch:$('branchName').value,from:$('branchFrom').value}));
$('loadFile').onclick=safe(async()=>{if(fileDirty&&!confirm('重新加载会放弃尚未保存的编辑，是否继续？'))return;const f=await api('file?story='+encodeURIComponent(current)+'&path='+encodeURIComponent($('fileList').value));$('filePath').value=f.path;$('editor').value=f.content;$('editor').readOnly=!f.editable;fileHead=f.head;fileDirty=false;$('fileState').textContent=f.editable?'已加载，可编辑。':'正文 / 系统回复为只读。';});
$('editor').oninput=()=>{fileDirty=true;$('fileState').textContent='有未保存的编辑';};$('filePath').oninput=()=>fileDirty=true;
$('newFile').onclick=()=>{if(fileDirty&&!confirm('放弃尚未保存的编辑？'))return;$('filePath').value='lore/new-place.yaml';$('editor').readOnly=false;$('editor').value='id: new-place\ntitle: 新地点\ncontent: |\n  在此描述这个地点。\n';fileHead=storyState?.head||'';fileDirty=true;};
async function save(remove=false){if(!current||!storyState)throw Error('请先选择故事。');if(!fileHead)throw Error('请先加载文件或新建 Lore。');if(remove&&!confirm('删除此设定并提交？历史版本仍会保留。'))return;await api('file',{story:current,path:$('filePath').value,content:$('editor').value,head:fileHead,remove});fileDirty=false;await refresh();fileHead=storyState.head;$('fileState').textContent=remove?'已删除，历史版本仍保留。':'已保存并提交。';if(remove){$('editor').value='';fileHead='';}notice('设定已更新。');}
$('saveFile').onclick=safe(()=>save());$('deleteFile').onclick=safe(()=>save(true));
window.addEventListener('beforeunload',e=>{if(fileDirty){e.preventDefault();e.returnValue='';}});
if(token)connect().catch(e=>notice(e.message));
</script></body></html>`;
